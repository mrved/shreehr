# Database
DATABASE_URL="postgresql://user:password@localhost:5432/shreehr"

# Redis (for BullMQ job queue)
REDIS_URL="redis://localhost:6379"

# Encryption (AES-256-GCM for PII fields)
ENCRYPTION_KEY=""

# NextAuth.js
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET=""

# Email (Resend)
RESEND_API_KEY=""
EMAIL_FROM="noreply@shreehr.local"

# AI Assistant (Claude or Ollama)
# Note: If ANTHROPIC_API_KEY is set, Anthropic will be used automatically
# You can override this by explicitly setting AI_PROVIDER
AI_PROVIDER=  # Optional: 'anthropic' or 'ollama' (auto-detects if not set)
ANTHROPIC_API_KEY=  # Your Anthropic API key (if using Claude)

# Ollama settings (used when AI_PROVIDER=ollama or for embeddings)
OLLAMA_BASE_URL="http://localhost:11434/api"
OLLAMA_MODEL="llama3.2:3b"
QDRANT_URL="http://localhost:6333"
EMBEDDING_MODEL="nomic-embed-text"

# Optional: External services
# AWS_ACCESS_KEY_ID=""
# AWS_SECRET_ACCESS_KEY=""
# AWS_REGION="ap-south-1"
