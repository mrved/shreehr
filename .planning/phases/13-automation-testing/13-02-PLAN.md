---
phase: 13-automation-testing
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - e2e/pages/employee.page.ts
  - e2e/pages/dashboard.page.ts
  - e2e/employee-crud.spec.ts
autonomous: true

must_haves:
  truths:
    - "Admin can create a new employee through the UI and see it in the list"
    - "Admin can view employee details page"
    - "Admin can edit employee information"
    - "Employee list supports search/filter"
  artifacts:
    - path: "e2e/pages/employee.page.ts"
      provides: "Page object for employee list and form pages"
      min_lines: 60
    - path: "e2e/pages/dashboard.page.ts"
      provides: "Page object for admin dashboard with sidebar navigation"
      min_lines: 30
    - path: "e2e/employee-crud.spec.ts"
      provides: "E2E tests for employee create, view, edit, search"
      min_lines: 100
  key_links:
    - from: "e2e/employee-crud.spec.ts"
      to: "e2e/pages/employee.page.ts"
      via: "page object import"
      pattern: "import.*EmployeePage"
    - from: "e2e/employee-crud.spec.ts"
      to: "e2e/fixtures/base.ts"
      via: "test fixture import for admin auth"
      pattern: "userRole.*admin"
---

<objective>
Create page objects for admin dashboard and employee management, then write E2E tests for employee CRUD operations.

Purpose: Employee management is the core data entity of the HRMS. CRUD operations must work flawlessly as all other modules depend on employee records.
Output: EmployeePage and DashboardPage page objects, and employee-crud.spec.ts covering create, view, edit, and search.
</objective>

<execution_context>
@C:\Users\ved\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\ved\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@e2e/fixtures/base.ts
@e2e/pages/login.page.ts
@src/app/dashboard/employees/page.tsx
@src/app/dashboard/employees/new/page.tsx
@src/app/dashboard/employees/[id]/page.tsx
@src/app/dashboard/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DashboardPage and EmployeePage page objects</name>
  <files>
    e2e/pages/dashboard.page.ts
    e2e/pages/employee.page.ts
  </files>
  <action>
1. Create `e2e/pages/dashboard.page.ts`:
   - Constructor takes Page, stores it
   - Locators for sidebar navigation links: use `page.getByRole('link', { name: ... })` for items like "Employees", "Attendance", "Leave", "Payroll", "Approvals", "Policies"
   - `async navigateTo(section: string)` method: clicks the sidebar link matching section name, waits for navigation
   - `async getPageHeading()`: returns text of first h1/h2 on page
   - `async isSidebarVisible()`: checks if nav/sidebar element is visible

2. Create `e2e/pages/employee.page.ts`:
   - Constructor takes Page
   - Locators:
     - `addEmployeeButton`: button or link with text "Add Employee" or "New Employee" or "Create"
     - `employeeTable`: table or list container for employee records
     - `searchInput`: input with placeholder containing "Search" or name="search"
     - Form fields (for /dashboard/employees/new):
       - `firstNameInput`: input for first_name (use label, name, or data-testid)
       - `lastNameInput`: input for last_name
       - `emailInput`: input for personal email or work email
       - `phoneInput`: input for personal_phone
       - `employeeCodeInput`: input for employee_code
       - `dobInput`: input for date_of_birth
       - `genderSelect`: select or combobox for gender
       - `dateOfJoiningInput`: input for date_of_joining
       - `departmentSelect`: select/combobox for department
       - `designationSelect`: select/combobox for designation
       - `addressInput`: input/textarea for address_line1
       - `cityInput`: input for city
       - `stateInput`: input/select for state
       - `postalCodeInput`: input for postal_code
       - `submitButton`: button[type="submit"] or button with text "Save"/"Create"
     - `successMessage`: element with success alert role or toast
     - `errorMessage`: element with error alert role

   - Methods:
     - `async goto()`: navigate to /dashboard/employees
     - `async gotoNew()`: navigate to /dashboard/employees/new
     - `async fillEmployeeForm(data: EmployeeFormData)`: fills all form fields. EmployeeFormData is an interface with firstName, lastName, phone, employeeCode, dob, gender, dateOfJoining, department, designation, address, city, state, postalCode
     - `async submitForm()`: clicks submit button
     - `async searchEmployee(query: string)`: fills search input, waits for results to update
     - `async getEmployeeCount()`: counts rows in employee table/list
     - `async clickEmployee(name: string)`: clicks on employee row/card matching name
     - `async getEmployeeDetails()`: returns object with visible detail fields (name, code, department, etc.)

   NOTE: Locators should be resilient. Try data-testid first, then aria labels, then semantic selectors, then CSS as last resort. Read the actual page components to determine correct selectors.
  </action>
  <verify>
    `pnpm exec tsc --noEmit` passes. Both page object files exist and export their classes.
  </verify>
  <done>
    - DashboardPage provides sidebar navigation methods
    - EmployeePage provides form fill, search, and list interaction methods
    - Both use resilient locator strategies (data-testid > aria > semantic > CSS)
  </done>
</task>

<task type="auto">
  <name>Task 2: Write employee CRUD E2E tests</name>
  <files>
    e2e/employee-crud.spec.ts
  </files>
  <action>
Create `e2e/employee-crud.spec.ts` using `test` and `expect` from `./fixtures/base`:

All tests use `userRole: 'admin'` since only admins/HR can manage employees.

```
describe('Employee CRUD Operations')
  describe('Employee List')
    - 'should display employee list page'
      -> navigate to /dashboard/employees, assert page heading contains "Employees", assert table/list is visible
    - 'should show seeded test employees in the list'
      -> navigate to /dashboard/employees, assert at least 5 employees visible (the E2E test users)
    - 'should filter employees by search query'
      -> navigate to /dashboard/employees, search for "Test Admin", assert filtered results contain "Test Admin", assert count is reduced

  describe('Create Employee')
    - 'should navigate to create employee form'
      -> navigate to /dashboard/employees, click add employee button, assert URL contains '/new' or form is visible
    - 'should create a new employee with valid data'
      -> navigate to create form, fill form with unique test data (employee_code: `E2E_TEST_${Date.now()}`, first_name: "Playwright", last_name: "Test"), submit, assert success message or redirect to employee detail/list
    - 'should show validation errors for missing required fields'
      -> navigate to create form, click submit without filling required fields, assert error messages visible

  describe('View Employee')
    - 'should display employee details page'
      -> navigate to /dashboard/employees, click on a seeded test employee ("Test Admin"), assert URL contains employee ID pattern, assert employee details visible (name, code, department)

  describe('Edit Employee')
    - 'should update employee information'
      -> navigate to employee detail, find edit button/link, update a field (e.g., city), save, assert success message, verify updated value
```

Important implementation notes:
- Generate unique employee codes using `E2E_TEST_${Date.now()}` to avoid conflicts
- Use `page.waitForResponse` or `page.waitForURL` after form submissions to handle async API calls
- For selects/comboboxes: read actual component implementation to determine interaction pattern (Radix UI select, native select, etc.)
- Clean up created test employees in afterAll hook by noting the employee code and deleting via API or database
- Add `test.describe.configure({ mode: 'serial' })` if tests depend on order (create -> view -> edit)
  </action>
  <verify>
    `pnpm exec tsc --noEmit` passes. File has at least 8 test cases. Run `pnpm test:e2e --project=chromium e2e/employee-crud.spec.ts` if app is running.
  </verify>
  <done>
    - employee-crud.spec.ts covers list view, search, create, view detail, edit
    - Tests use EmployeePage and DashboardPage page objects
    - Unique test data generated per run to avoid conflicts
    - At least 8 test cases with explicit assertions
  </done>
</task>

</tasks>

<verification>
1. `pnpm exec tsc --noEmit` passes
2. DashboardPage has navigateTo method for sidebar navigation
3. EmployeePage has form fill, search, and list interaction methods
4. employee-crud.spec.ts has 8+ test cases covering CRUD + search
5. Tests use page object pattern throughout
</verification>

<success_criteria>
- Page objects for dashboard and employee management created with resilient locators
- E2E tests cover full employee CRUD lifecycle: list, search, create, view, edit
- Tests generate unique data per run to avoid conflicts
- All tests use admin role authentication
</success_criteria>

<output>
After completion, create `.planning/phases/13-automation-testing/13-02-SUMMARY.md`
</output>
